# Production API — Mail body encryption (secrets manager / ECS task env)
# Provision CMK first: ./scripts/provision-mail-body-kms.sh production

MAIL_AWS_REGION=eu-north-1
MAIL_KMS_KEY_ID=alias/rukny-mail-body-encryption

# Global kill switch — enable only during controlled rollout waves (Doc 3 P6)
MAIL_BODY_ENCRYPTION_ENABLED=false

# After pilot soak: false — new mail is ENCRYPTED without plaintext columns
MAIL_BODY_ENCRYPTION_DUAL_WRITE=false

MAIL_BODY_ENCRYPTION_DEV_FALLBACK=false
