# Staging API — Mail body encryption (paste into secrets manager / ECS task env)
# Provision CMK first: ./scripts/provision-mail-body-kms.sh staging

MAIL_AWS_REGION=eu-north-1
MAIL_KMS_KEY_ID=alias/rukny-mail-body-encryption-staging

# Keep false until pilot app is selected and staging deploy is ready
MAIL_BODY_ENCRYPTION_ENABLED=false

# Staging pilot: true during dual-write soak; false after null-plaintext gate
MAIL_BODY_ENCRYPTION_DUAL_WRITE=true

# Never set in staging/production
MAIL_BODY_ENCRYPTION_DEV_FALLBACK=false
