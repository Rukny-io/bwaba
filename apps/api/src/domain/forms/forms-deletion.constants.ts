/** Days before soft-deleted forms and their submissions are permanently removed. */
export const FORM_DELETION_RETENTION_DAYS = 30;

/** Max deletion reason length (sanitized). */
export const FORM_DELETION_REASON_MAX_LENGTH = 500;
