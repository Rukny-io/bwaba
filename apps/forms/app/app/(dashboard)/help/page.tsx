import { HelpView } from '@/components/help/help-view';

export default function HelpPage() {
  return (
    <section className="dashboard-page flex flex-col gap-3.5 sm:gap-6">
      <HelpView />
    </section>
  );
}
