import { PlaceholderPage } from '@/components/app/placeholder-page';

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="الإعدادات"
      description="إعدادات Workspace، الفوترة، وحدود الباقة — تُربط لاحقاً مع accounts.rukny.io/manage/billing."
    />
  );
}
