// Providers - Shared authentication providers
'use client';

export {
  SharedAuthProvider,
  useSharedAuth,
} from './shared-auth-provider';

export type {
  AuthState,
  AuthContextValue,
  SharedAuthProviderProps,
} from './shared-auth-provider';

export { default } from './shared-auth-provider';
